export const stringToUpper = (str: string) => {
  return str.toUpperCase();
};

export const stringTrim = (str: string) => {
  return str.trim().replaceAll(" ", "");
};

export const stringToLower = (str: string) => {
  return str.toLowerCase();
};
